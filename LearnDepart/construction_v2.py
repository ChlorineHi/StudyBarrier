### 深度学习代码顺序

1. **数据预处理**：
   - 使用数据预处理模块对输入数据进行处理，包括归一化、过滤低计数基因和细胞等。

2. **模型初始化**：
   - 创建模型实例，设置输入维度、潜在维度、聚类数量、编码器和解码器的层数、激活函数、温度参数和正则化参数等。

3. **聚类中心初始化**：
   - 在训练的初始阶段，使用 KMeans 等方法初始化聚类中心。

4. **前向传播**：
   - 在每个训练周期中，将输入数据传递通过模型的前向传播，计算潜在表示、软分配结果和重构参数。

5. **计算损失**：
   - 计算不同类型的损失，包括：
     - **重构损失**（使用 ZINB损失计算）。
     - **聚类损失**（通过聚类标签和预测的软标签计算）。
     - **对比损失**（通过对比学习模块计算）。

6. **反向传播与优化**：
   - 根据计算得到的损失进行反向传播，并更新模型参数。

7. **评估模型性能**：
   - 在每个训练周期结束后，使用评估指标（如准确率、F1分数等）评估模型的性能。

8. **保存模型**：
   - 根据需要保存模型的状态，以便后续使用或进一步训练。
